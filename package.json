{
  "name": "print-agent",
  "version": "1.0.8",
  "description": "Aplicativo desktop de ponte local para impressao via localhost.",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "pack": "electron-builder --dir",
    "dist:win": "electron-builder --win nsis",
    "release:win": "electron-builder --win nsis --publish always",
    "release": "powershell -ExecutionPolicy Bypass -File scripts/release.ps1",
    "sync": "powershell -ExecutionPolicy Bypass -File scripts/release.ps1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "type": "commonjs",
  "devDependencies": {
    "electron": "^41.3.0",
    "electron-builder": "^26.8.1"
  },
  "dependencies": {
    "electron-log": "^5.4.3",
    "electron-updater": "^6.8.3"
  },
  "build": {
    "appId": "br.com.dalmago.printagent",
    "productName": "Print Assistant",
    "artifactName": "PrintAssistantSetup.${ext}",
    "asar": true,
    "directories": {
      "buildResources": "build",
      "output": "dist"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "precifybr",
        "repo": "print-agent"
      }
    ],
    "files": [
      "main.js",
      "preload.js",
      "index.html",
      "package.json",
      "build/icon.png",
      "build/icon.ico"
    ],
    "win": {
      "icon": "build/icon.ico",
      "signAndEditExecutable": false,
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "executableName": "Print Assistant"
    },
    "nsis": {
      "artifactName": "PrintAssistantSetup.${ext}",
      "installerIcon": "build/icon.ico",
      "uninstallerIcon": "build/icon.ico",
      "oneClick": false,
      "perMachine": true,
      "allowElevation": true,
      "allowToChangeInstallationDirectory": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Print Assistant",
      "deleteAppDataOnUninstall": false
    }
  }
}
